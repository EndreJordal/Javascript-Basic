//console.log("Hello World!!!");

// To måter å lage en variabel på - 'const' og 'let'

const constVariabel = "Konstant. Kan ikke endres.";

// Operator (+, -, *, /)
// Shorthand operators (+=, -=, *=, /*, ++, --)
let lives = 6;
//lives = lives + 1;
//lives += 1;

// Increment / Decrement
lives++; //lives--
//console.log(constVariabel);

//Data typer:

//String

const string = "Bare tekst. Bruk hermetegn.";
const name = "Bengt Leiknes";

//Numbers / Integers(Int) / Float (Floating point number)

const number = 45.312;

//Boolean

const boolean = false;
const darkModeToggle = false; //Camel case.

// Array - en liste.

const weekdays = [
  "Mandag",
  "Tirsdag",
  "Onsdag",
  "Torsdag",
  "Fredag",
  "Lørdag",
  "Søndag",
];

const colors = ["Red", "Blue", "Yellow"];

console.log(weekdays[5], weekdays[6]);
console.log(weekdays[0]);
