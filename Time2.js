//console.log("Hei");

// const og let

const variableName = "Dette er tekst";
//let lives = 5

// Operators +, -, *, /
// Shorthand operators +=, -=, ++, --

/* lives = lives + 1
lives += 1
lives++ */

// Datatyper

// String. Numbers. Boolean. Arrays.

// Conditionals. IF statement.

// Hvis... -> IF

// Variable that stores how many lives the player has.
let lives = 1;

// Comparison operator <, >, ===, (==)
//console.log(lives === 5);
/* if (lives > 0) {
  console.log("Player is NOT game over :D");
} else {
  console.log("Player is game over :(");
} */

//console.log("5" === "5");

const userName = "Tomas"; //""
const userAge = 19;
const userIsAdmin = false;

// Logical AND / Logical OR

// Truthy / Falsey

// Template literals - backticks og ${}

/* if (userAge >= 18) {
  console.log(`Welcome ${userName}, you are ${userAge} years old`);
} else if (userIsAdmin) {
  console.log("Welcome Admin!");
} else {
  console.log("User access denied!");
} */
//console.log("ABC" + "DEF");

// Øl? Eller saft? "Øl" / "Saft"
/* let drink = "";
if (userAge >= 18) {
    drink = "Øl";
} else {
    drink = "Saft";
} */
// Ternary - forkortet if/else
// påstand ? "Dette returnes hvis true" : "Dette returnes hvis false"
const drink = userAge >= 18 ? "Øl" : "Saft";
console.log(userAge >= 18 ? "Øl" : "Saft");
const variable = 4 + 4;
console.log(4 + 4);
